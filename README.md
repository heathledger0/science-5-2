# 단원 열기 — 과학 단원 도입 활동 도우미

초등 과학 수업에서 새 단원을 시작할 때, 그 단원의 핵심 개념을 전체적으로 훑어보는 도입 활동을 위한 웹앱입니다.
서버나 로그인 없이 브라우저에서 바로 동작하고, 데이터는 사용하는 기기(브라우저)에만 저장됩니다.

## 왜 만들었나요

기존에는 "10분간 교과서를 읽고 중요한 개념을 찾은 뒤, 교사가 미리 뽑아 둔 목록과 몇 개나 일치하는지 확인하는" 방식으로
단원을 도입했습니다. 이 앱은 그 방식을 더 쓰기 편하게 만들고, 같은 목적(핵심 개념 전체 조망)을 가진 다른 활동도
함께 제공해서 그날그날 다르게 선택할 수 있게 합니다.

## 사용 흐름

1. **단원 준비하기** — 교과서 본문(또는 목차, 요약)을 붙여넣거나 `.txt` / `.pdf` / `.hwp` / `.hwpx` 파일을 올립니다.
   업로드하면 브라우저 안에서 바로 텍스트를 뽑아 입력창에 채워줍니다(외부 서버로 전송되지 않음).
   - PDF는 스캔한 이미지가 아니라 글자 정보가 있는 파일이어야 인식돼요.
   - 한글(HWP/HWPX) 파일은 별도 오픈소스 라이브러리로 처리하는데, 100% 완벽하지는 않을 수 있어요.
     추출된 내용이 이상하면 원본에서 복사해서 붙여넣는 방식으로 바꿔서 시도해 주세요.
   "핵심 개념 후보 찾기"를 누르면 두 가지 방식으로 후보를 제안합니다.
   - 🔑 정의 문장 인식: "OOO라고 한다/부른다"처럼 개념을 정의하는 문장에서 뽑은 단어 (정확도 높음)
   - 📊 빈도 기반: 본문에 자주 등장하는 단어
   - 자동 추출은 100% 정확하지 않습니다. **최종적으로 어떤 개념을 정답으로 쓸지는 항상 교사가 클릭해서 선택**합니다.
     후보에 없는 개념은 직접 입력해서 추가할 수 있습니다.
2. **활동 선택하기** — 확정된 핵심 개념으로 아래 4가지 활동 중 그날 쓸 것을 고릅니다.
   - 🕵️ **개념 일치 확인 게임**: 기존에 하시던 방식의 개선판. 타이머로 탐색 시간을 재고, 모둠별로 찾은 개념을 입력하면
     정답과 자동 비교해 모둠별 순위를 보여줍니다. 오타로 보이는 답은 교사가 인정 여부를 직접 결정합니다.
   - 🎯 **개념 빙고**: 핵심 개념(+ 헷갈리는 용어)으로 빙고판을 만들어 교과서에서 찾아 표시합니다. 학생마다 다른 배치의
     인쇄용 카드를 만들 수 있습니다.
   - 🔤 **초성 퀴즈**: 핵심 개념의 초성만 보고 맞히는 워밍업 게임.
   - 📝 **KWL 차트**: 개념을 공개하지 않고 "이미 아는 것 / 알고 싶은 것 / 배운 것"을 정리하며 스스로 궁금증을 갖게 합니다.
3. 준비한 단원은 홈 화면에 계속 남아 있어서, 다음 시간에도 바로 이어서 쓸 수 있습니다.

## 다른 선생님과 공유하기

이 앱은 로그인이 없고 데이터는 각자의 브라우저에만 저장됩니다. 준비한 단원을 다른 선생님과 공유하려면
홈 화면의 **내보내기** 버튼으로 `.json` 파일을 받아 전달하고, 받은 선생님은 **파일로 불러오기**로 추가하면 됩니다.

## 개인정보 및 오프라인 사용

- 붙여넣거나 업로드한 교과서 내용, 학생 입력 내용은 외부 서버로 전송되지 않고 브라우저 안에서만 처리됩니다.
  PDF/한글(HWP·HWPX) 파일 처리도 이 저장소에 함께 담겨 있는 라이브러리로 브라우저 안에서 처리하며,
  파일 내용이 어디로도 업로드되지 않습니다.
- AI API를 사용하지 않으므로 별도 API 키 설정이나 사용료 없이 누구나 바로 쓸 수 있습니다.
- 처음 페이지를 불러올 때 필요한 파일은 함께 배포되므로, 이후에는 새로고침 전까지 인터넷 연결 없이도 대부분 동작합니다.

## GitHub Pages로 배포하기 (관리자용)

1. 이 브랜치를 저장소의 기본 브랜치(예: `main`)로 병합합니다.
2. GitHub 저장소의 **Settings → Pages**로 이동합니다.
3. **Source**를 "Deploy from a branch"로 설정하고, 배포할 브랜치(예: `main`)와 폴더(`/root`)를 선택한 뒤 저장합니다.
4. 잠시 후 `https://<계정 또는 조직>.github.io/<저장소 이름>/` 주소로 접속할 수 있습니다.
5. 이 링크를 다른 선생님들과 공유하면 별도 설치 없이 브라우저에서 바로 사용할 수 있습니다.

## 로컬에서 미리보기

빌드 도구 없이 순수 HTML/CSS/JS(ES 모듈)로 되어 있어서, 정적 파일 서버로 열면 됩니다.

```bash
python3 -m http.server 8000
# 브라우저에서 http://localhost:8000 접속
```

(ES 모듈은 `file://`로 직접 열면 브라우저 보안 정책 때문에 동작하지 않을 수 있어, 반드시 로컬 서버나
GitHub Pages 같은 http(s) 환경에서 열어주세요.)

## 폴더 구조

```
index.html          앱 진입점
style.css            전체 스타일 (인쇄 스타일 포함)
js/app.js            해시 라우터 (#/, #/prepare, #/play/:id, #/play/:id/:mode)
js/store.js           localStorage 기반 저장소
js/extract.js         핵심 개념 후보 자동 추출 (규칙 기반, AI 미사용)
js/hangul.js           한글 초성 분해 유틸
js/util.js             공통 유틸 (문자열 비교, 셔플, 효과음 등)
js/views/home.js        홈(단원 목록)
js/views/prepare.js      단원 준비(핵심 개념 추출·확정)
js/views/playSelect.js   활동 모드 선택
js/views/match.js        개념 일치 확인 게임
js/views/bingo.js        개념 빙고
js/views/chosung.js       초성 퀴즈
js/views/kwl.js           KWL 차트
js/fileExtract.js       업로드 파일(.txt/.pdf/.hwp/.hwpx)에서 텍스트 추출
js/vendor/pdfjs/         PDF 텍스트 추출용 (Mozilla pdf.js, Apache-2.0)
js/vendor/hwp-convert.browser.mjs   한글(HWP/HWPX) 텍스트 추출용 (hwp-convert, MIT)
```

`js/vendor/` 아래 라이브러리는 준비 화면에서 PDF나 한글 파일을 올릴 때만 필요한 시점에 불러옵니다(초기
페이지 로딩을 무겁게 하지 않기 위함). 각 라이브러리의 라이선스 고지는 `js/vendor/hwp-convert.LICENSE.txt`,
`js/vendor/hwp-convert.NOTICE.txt`에 있고, pdf.js는 파일 상단 주석에 Apache-2.0 고지가 포함되어 있습니다.

## 앞으로 더할 수 있는 것

- 개념 지도(마인드맵) 그리기 활동
- 여러 기기 간 자동 동기화 (현재는 파일 내보내기/불러오기로 수동 공유)
